# Constraint.msg
# this contains an array of stamped edges where an actor cannot be in the time stamp range

float64[2] stamp            # initial and terminal times of the edge transition, assume corresponding edge is associated via index in array