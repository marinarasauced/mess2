# Segment.msg
# this contains an edge that is stamped with the times during which an actor completes the respective edge transition from source to target

int64 index                 # index of the edge
float64[2] stamp            # initial and terminal times of the edge transition